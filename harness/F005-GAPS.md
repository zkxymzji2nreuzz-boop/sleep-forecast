# F005 Gap List — Generator Punch List

Generated by Planner Agent on 2026-04-10.
Read this file alongside `harness/F005-SPEC.md` before writing any code.

---

## What Is Already Complete (do NOT re-implement)

| Item | File |
|---|---|
| All packages installed | `package.json` (gray-matter, remark stack, @ducanh2912/next-pwa, next-sitemap, @tailwindcss/typography, rehype-slug, unified) |
| ArticleMeta / ArticleFull types | `src/lib/types.ts` |
| articles.ts — all 4 functions | `src/lib/articles.ts` |
| Article detail page | `src/app/articles/[slug]/page.tsx` (generateStaticParams, generateMetadata, JSON-LD, ArticleLayout) |
| Article index page | `src/app/articles/page.tsx` |
| ArticleLayout component | `src/components/ArticleLayout.tsx` (prose, CTA → /record, related articles, medical disclaimer) |
| 3 Markdown articles | `src/content/articles/{kiatsu-zutsu,suimin-shitsu-up,tsuki-to-suimin}.md` |
| next.config.mjs PWA wrap | `next.config.mjs` (withPWA, disable in dev, skipWaiting, clientsClaim) |
| manifest.json | `public/manifest.json` |
| layout.tsx PWA meta | `src/app/layout.tsx` (manifest, appleWebApp, icons, OGP, Twitter Card, metadataBase) |
| next-sitemap.config.js | `next-sitemap.config.js` (generateRobotsTxt, Disallow /api/) |
| postbuild script | `package.json` scripts.postbuild = "next-sitemap" |
| .gitignore SW entries | `.gitignore` (sw.js, workbox-*, sitemap.xml, robots.txt) |
| Footer Articles link | `src/components/Footer.tsx` |
| @tailwindcss/typography | `tailwind.config.ts` plugins |

---

## Gap 1 — PWA Icons (BLOCKING for AC-5)

The spec requires three icon files in `public/`:

- `public/icon-192.png` (192x192)
- `public/icon-512.png` (512x512)
- `public/apple-touch-icon.png` (180x180)

**Current state:** `manifest.json` and `layout.tsx` reference these paths, but the actual PNG files may be placeholder/empty or missing. The build will succeed but Lighthouse PWA audit and Chrome DevTools Manifest panel will show icon errors.

**Action required:**
- Verify whether `public/icon-192.png`, `public/icon-512.png`, and `public/apple-touch-icon.png` are valid PNG files (non-zero byte count, readable image headers).
- If any are missing or zero-byte: generate minimal valid PNGs. Acceptable for MVP: solid `#0f1117` background with a white moon-crescent SVG rendered via sharp, OR copy a suitable existing icon from public assets. The exact visual does not matter for AC-5 as long as Chrome DevTools Manifest shows them without errors.
- Do NOT change manifest.json icon paths — they are already correct.

---

## Gap 2 — OGP Default Image (BLOCKING for AC-3 / Lighthouse)

The spec (section 4-F) requires `public/og-default.png` at 1200x630px.
`layout.tsx` and `src/app/articles/[slug]/page.tsx` already reference `/og-default.png` via `openGraph.images`.

**Current state:** File likely absent. Missing og:image causes Lighthouse Best Practices deductions and broken social previews.

**Action required:**
- Create `public/og-default.png` (1200x630). Acceptable MVP content: dark background (#0f1117) with white text "SleepForecast | 眠れる明日予報". Can be generated with sharp or any image tool.
- No dynamic generation required (F006+ scope).

---

## Gap 3 — Article Word Count Verification (AC-2)

Spec requires each article to be 1500+ characters (2000 ideal) with 3+ H2 headings.

**Action required:**
- Count body characters (excluding frontmatter) for each `.md` file.
- If any article is under 1500 characters, extend it until it reaches the threshold.
- Confirm each article has at least 3 `## ` headings in the body.
- Confirm each article's body ends with the medical disclaimer sentence: `本記事は医療行為・診断を目的としたものではありません。`
  (Check the existing text matches this exact requirement from spec section 1-H.)

---

## Gap 4 — `npm run build` Verification (BLOCKING — do this last)

Several WIP files are modified but not yet committed. TypeScript errors could be lurking.

**Action required:**
- Run `npm run build` from the project root.
- Fix any TypeScript strict-mode errors or ESLint warnings until both pass cleanly.
- Common risk area: `remark-parse` is in the unified pipeline in `articles.ts` — confirm it is installed (it ships with `remark` but verify no "Cannot find module" at build time).
- Confirm `public/sw.js` exists after build (next-pwa output). If it does not appear, check that `NODE_ENV` is not being set to `development` during the build command.

---

## Gap 5 — sitemap.xml Contains Article URLs (AC-4)

`next-sitemap.config.js` excludes `/api/*` and uses `additionalPaths` is NOT configured — the default crawler-based approach should pick up `/articles/*` pages from the static build output.

**Action required:**
- After `npm run build`, run `npm run postbuild` (or rely on the automatic postbuild hook).
- Inspect `public/sitemap.xml` (it will be generated there and then git-ignored; check the content before committing).
- Confirm the XML contains entries for `/articles/kiatsu-zutsu`, `/articles/suimin-shitsu-up`, `/articles/tsuki-to-suimin`.
- If these URLs are absent, add an `additionalPaths` callback in `next-sitemap.config.js` that enumerates the three slugs explicitly. See spec section 1-F for pattern.

---

## Gap 6 — robots.txt `Disallow: /api/` (AC-4)

The `next-sitemap.config.js` has `disallow: ["/api/"]` in robotsTxtOptions. However, AC-4 states the generated `robots.txt` must contain `Disallow: /api/`.

**Action required:**
- After postbuild, cat `public/robots.txt` and confirm `Disallow: /api/` is present.
- No code change needed if next-sitemap config is correct, but verify the output.

---

## Gap 7 — .gitignore Does Not Cover `public/og-default.png` or Icons

The icons and og-default.png that will be created in Gap 1 and Gap 2 SHOULD be committed to the repository (they are static assets, not generated files). Make sure `.gitignore` does NOT accidentally exclude them.

**Action required:**
- Confirm that `public/icon-192.png`, `public/icon-512.png`, `public/apple-touch-icon.png`, and `public/og-default.png` are NOT matched by any `.gitignore` pattern.
- Current `.gitignore` patterns for `public/` are specific to sw.js/workbox/sitemap/robots — icons and og image should be safe, but verify.

---

## Gap 8 — Commit All WIP

After all gaps above are resolved:

- Stage and commit all modified and new files with message: `feat: F005 SEO article system + PWA (WIP → complete)`
- Update `harness/feature-list.json` F005 `status` field only after Evaluator passes (do not touch status yourself).
- Append a progress note to `harness/claude-progress.txt`.

---

## Non-Goals (Do NOT do in this session)

- Do not add `additionalPaths` to sitemap unless Gap 5 verification shows articles are missing.
- Do not implement `/about`, `/privacy`, `/terms`, `/contact` (F006 scope).
- Do not add GA4 tracking code (F006 scope).
- Do not implement `AdBanner.tsx` (F006 scope).
- Do not add dynamic OGP image generation (F007+ scope).
- Do not add BreadcrumbList JSON-LD (F006 scope).
- Do not customize `runtimeCaching` in next-pwa (explicitly out of scope per spec section 1-E).

---

*Planner Agent — 2026-04-10*
